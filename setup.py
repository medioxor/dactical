from setuptools import setup, find_packages

setup(
    name="dactical",
    version="0.1",
    packages=find_packages(),
    license="",
    author="",
    author_email="",
    description="Detection as Code",
    package_data={},
    entry_points={
        'console_scripts': [
        ],
    }
)
